# Roadmap

créer un cli nommer `eorbahapi`.

Ajout d'un dossier `public/` pour y mettre les fichier à servir globalement via `/`

Créer un serveur dev pour eorbahapi `eorbahapi dev` pour allumer un serveur et `eorbahapi build` pour build l'app mettre les fichier public à la racine etc